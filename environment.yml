name: bubbleblower
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.12.14
  - setuptools=84.0.0
  - pytest=9.1.1
  - pytest-cov=7.1.0
  - numpy=2.5.3
  - pyyaml=6.0.3
  - pip
  - pip:
      # Editable checkout: metametro reads VERSION two directories above the
      # package (the repository root). A wheel leaves that file out, so import fails.
      - "-e git+https://github.com/dsmutin/MetaMetro.git@ac4218954dde455b8604f79cd0f416425540bbdc#egg=metametro"
variables:
  PYTHONPATH: src
